// notes.js
let someArray = [0, 1, 2, 3, 4, 5, true];

let someString = 'some random string';

//   starting index configuration; index < array.length; index++
const forLoopFx = (itter) => {
     // all parameters are treated like local variables within their function scope
     // console.log(arguments)

     for (let i = 0; i < itter.length; i++) {
          // logic to be enacted on every loop
          console.log(itter[i])
     };
};

// forLoopFx(someArray,3, 4, 5, 6, 7,8 , 89, 76, 45);
// forLoopFx(someString) // s o m e  r a n d


// console.log(document)

// window
// document

// HTMLElement.addEventListener('onClick', () => {
//      // do this logice when a click event happens on the objectRecieveerHTMLELEMtn
// })

// <----------------------------------------------------------------------------------------------------------------------------->
// return the first elemtn in an array
// create a fx that takes in an array aprameter
// find the first indicie
// print that or return out once found


// TDD
// DO NOT use destructive coding 

// returning the last indicie instead of the first
// const someFunction = (arr) => {
//    // somestuff
//
//   return 'whateevr
// }


// [https://stackoverflow.com/questions/35530930/nodemon-not-working-bash-nodemon-command-not-found]
// not im returning the negative of the first indice
// const someFunction = (arr) => {
//      // does some stuff
//      return 'that stuff'
// }

// 
const someFunction = (arr) => {
     // does some stuff

     return 'that stuff'
}


// a action verb, the language, the problem in pseduodocode or plain english AND the eror message or code snippet





































{/* <--------------------------------------------------------------------------------------------------> */}